{
  "name": "ar-pmp-question-bank",
  "version": "0.1.0",
  "description": "PMP question bank inspired by the Andrew Ramdayal (AR) Udemy course — structured, validated, and open source.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-standalone.mjs",
    "validate": "node scripts/validate-questions.mjs",
    "quiz": "node scripts/quiz.mjs",
    "test": "node --test scripts/test/"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
